public enum StateIndex { Gallery, TRAP, PATROL, CHASE, ATTACK, JUMP, STURN, Flaying };

public enum AttackKind { HAND_ATTACK, RUSH_ATTACK };

public enum ZombieType { Gallery, Stoker, Falldown }

public enum ItemType { KeyItem, BonusItem, Gold, Count = 3 }

public enum Direction { Left, Right, Foward, Back }

public enum EquipType { Head, Body, Shose }